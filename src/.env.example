# ===== ตั้งค่า Frontend — คัดลอกเป็น src/.env =====

# URL ฐานของ API — เว้นว่างได้ถ้าหน้าเว็บกับ backend อยู่ origin เดียวกัน (เรียก /api ตรงๆ)
# ตั้งค่าเมื่อ dev แยกพอร์ต เช่น http://localhost:5000
VITE_API_URL=
